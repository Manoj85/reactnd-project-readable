import { combineReducers } from 'redux'

function post(state = {}, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default combineReducers({
    post

})